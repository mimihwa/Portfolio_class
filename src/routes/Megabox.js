import React from 'react';
import Header from '../conponents/Header';

const megabox = (props) => {
    return (
        <div>
            <Header />
            메가박스
        </div>
    );
};

export default megabox;